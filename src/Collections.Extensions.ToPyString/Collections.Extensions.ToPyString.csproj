<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <PackageId>Collections.Extensions.ToPyString</PackageId>
    <Version>1.1.4</Version>
    <Authors>Nenad Misic</Authors>
    <ReleaseNotes>Refactor the converters</ReleaseNotes>
    <PackageDescription>.NET System.Collections ToString extension for converting collections (and other types!) to a string in the format that is based on the one in Python</PackageDescription>
    <Description>.NET System.Collections ToString extension for converting collections (and other types!) to a string in the format that is based on the one in Python</Description>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <copyright>Copyright 2020</copyright>
    <PackageTags>system;collections;extensions;tostring;topystring</PackageTags>
    <RepositoryUrl>https://github.com/misicnenad/topystring</RepositoryUrl>
    <PackageProjectUrl>https://misicnenad.github.io/topystring/</PackageProjectUrl>
    <PackageIconUrl>https://raw.githubusercontent.com/misicnenad/topystring/master/src/Collections.Extensions.ToPyString/images/string.png</PackageIconUrl>
    <PackageIcon>string.png</PackageIcon>
    <PackageReleaseNotes>Refactor the converters</PackageReleaseNotes>
  </PropertyGroup>

  <ItemGroup>
    <None Include="images\string.png" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>
